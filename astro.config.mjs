import { defineConfig } from 'astro/config';

// Set `site` to your real public URL for canonical links / OG when you add them.
export default defineConfig({
  site: 'https://your-site.example.com',
});
